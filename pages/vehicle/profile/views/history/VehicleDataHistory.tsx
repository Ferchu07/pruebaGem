import React from "react";
import VehicleMetrics from "../metrics/VehicleMetrics";

const VehicleDataHistory: React.FC = () => {
  return <VehicleMetrics view="viewer-only" />;
};

export default VehicleDataHistory;
